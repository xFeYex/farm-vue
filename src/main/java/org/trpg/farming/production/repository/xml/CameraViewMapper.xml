<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.production.dao.CameraViewRepository">

    <select id="findByResourceId" resultType="com.example.production.entity.CameraView">
        SELECT
            id,
            resource_id,
            camera_code,
            camera_name,
            camera_status,
            live_stream_url,
            last_cover_image_url,
            last_online_at,
            created_at,
            updated_at
        FROM production_camera_view
        WHERE resource_id = #{resourceId}
        LIMIT 1
    </select>

</mapper>