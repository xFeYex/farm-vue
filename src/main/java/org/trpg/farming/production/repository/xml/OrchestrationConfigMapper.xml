<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.production.dao.OrchestrationConfigRepository">

    <select id="findByResourceId" resultType="com.example.production.entity.OrchestrationConfig">
        SELECT
            id,
            resource_id,
            subscription_id,
            orchestration_type,
            name,
            schedule_mode,
            times_per_day,
            duration_minutes,
            start_times_json,
            config_json,
            status,
            created_at,
            updated_at
        FROM production_orchestration_config
        WHERE resource_id = #{resourceId}
        LIMIT 1
    </select>

</mapper>